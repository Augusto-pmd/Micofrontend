# Portal Cliente UX — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Reemplazar el portal básico actual por la UX diseñada: pantalla de reserva con bloques Acceso Facial · Próximo Pago · Tu Espacio · Gestionar.

**Architecture:** Todo en `home.jsx` (patrón existente, no hay build step). Los nuevos componentes reemplazan `ReservationDetail`. El enrutamiento detecta cuántas reservas tiene el usuario y navega directo a la reserva si hay solo una. Los estados y flujos se guardan en localStorage (mismo store existente), con dos campos nuevos: `faceEnrollStatus` y el status `cancellation_scheduled`.

**Tech Stack:** React 18 CDN + Babel standalone + localStorage. Sin test runner — la verificación es en browser. Estilos en `styles.css`.

---

## Estructura de archivos

```
home.jsx          ← agregar/reemplazar: ReservationPortal, AccesoFacial,
                     FaceEnrollFlow, ProximoPago, TuEspacio, Gestionar,
                     ResizeModal, CancelConfirm, ReservationSelector.
                     Modificar: App routing, useHashRoute.
styles.css        ← agregar secciones de CSS al final del archivo.
```

No se crea ningún archivo nuevo. Todo sigue el patrón single-file existente.

---

## Task 1: CSS — nuevos componentes del portal

**Files:**
- Modify: `styles.css` (agregar al final)

- [ ] **Step 1.1: Agregar CSS de ReservationPortal y header con estados**

Al final de `styles.css` (después del bloque `@media (max-width: 390px)`) agregar:

```css
/* ════════════════════════════════════════════════════════════════ */
/* PORTAL v2 — Reservation Portal screen                              */
/* ════════════════════════════════════════════════════════════════ */

/* Page wrapper */
.mc-res-portal {
  max-width: 680px; margin: 0 auto;
  padding: 0 20px 80px;
}

/* Reservation header */
.mc-res-portal-header {
  display: flex; align-items: flex-start;
  justify-content: space-between; gap: 16px;
  padding: 32px 0 24px;
  border-bottom: 1px solid var(--mc-line-2);
  margin-bottom: 32px;
}
.mc-res-portal-header h1 {
  font-family: var(--font-cond); font-size: clamp(24px, 5vw, 32px);
  font-weight: 900; letter-spacing: -0.03em;
  margin: 0 0 6px;
}
.mc-res-portal-back {
  display: inline-flex; align-items: center; gap: 6px;
  font-family: var(--font-mono); font-size: 11px;
  letter-spacing: 0.1em; text-transform: uppercase;
  color: var(--mc-ink-3); font-weight: 600;
  margin-bottom: 12px;
}
.mc-res-portal-back:hover { color: var(--mc-violet); }

/* Status badges — portal version */
.mc-portal-status {
  display: inline-flex; align-items: center; gap: 6px;
  padding: 6px 12px; border-radius: 999px;
  font-family: var(--font-mono); font-size: 11px;
  font-weight: 700; letter-spacing: 0.06em;
  text-transform: uppercase; white-space: nowrap; flex-shrink: 0;
}
.mc-portal-status.active { background: var(--mc-green-soft); color: var(--mc-green-ink); }
.mc-portal-status.pending { background: #fef3c7; color: #92400e; }
.mc-portal-status.failed  { background: #fee2e2; color: #991b1b; }
.mc-portal-status.scheduled { background: #ffedd5; color: #9a3412; }
.mc-portal-status.cancelled { background: var(--mc-line); color: var(--mc-ink-3); }

/* Block shared */
.mc-portal-block {
  background: var(--mc-card);
  border: 1px solid var(--mc-line);
  border-radius: var(--mc-r-lg);
  padding: 24px;
  margin-bottom: 16px;
}
.mc-portal-block-title {
  font-family: var(--font-mono); font-size: 11px;
  letter-spacing: 0.12em; text-transform: uppercase;
  color: var(--mc-violet); font-weight: 700;
  margin-bottom: 16px; display: block;
}
```

- [ ] **Step 1.2: CSS bloque Acceso Facial (4 estados)**

```css
/* ── Block 1: Acceso facial ────────────────────────────────────── */
.mc-acceso { position: relative; overflow: hidden; }
.mc-acceso.state-active { border-color: color-mix(in oklab, var(--mc-green) 40%, transparent); }
.mc-acceso.state-failed  { border-color: color-mix(in oklab, #f59e0b 40%, transparent); }
.mc-acceso.state-processing { border-color: color-mix(in oklab, var(--mc-violet) 30%, transparent); }

.mc-acceso-icon {
  width: 44px; height: 44px; border-radius: 12px;
  display: inline-flex; align-items: center; justify-content: center;
  font-size: 22px; margin-bottom: 12px;
}
.mc-acceso-icon.lock    { background: var(--mc-violet-soft); }
.mc-acceso-icon.check   { background: var(--mc-green-soft); }
.mc-acceso-icon.warning { background: #fef3c7; }
.mc-acceso-icon.spin    { background: var(--mc-violet-soft); animation: spin 2s linear infinite; }
@keyframes spin { from { transform: rotate(0deg); } to { transform: rotate(360deg); } }

.mc-acceso h3 {
  font-family: var(--font-cond); font-size: 20px;
  font-weight: 900; letter-spacing: -0.02em;
  margin: 0 0 6px;
}
.mc-acceso p { font-size: 14px; color: var(--mc-ink-2); margin: 0 0 18px; line-height: 1.5; }
.mc-acceso .sub {
  font-family: var(--font-mono); font-size: 11px;
  color: var(--mc-ink-4); letter-spacing: 0.04em;
  margin-top: 12px; display: block;
}
.mc-acceso-actions { display: flex; gap: 10px; flex-wrap: wrap; }

/* Face enroll flow — 3 steps inline */
.mc-enroll {
  border-top: 1px solid var(--mc-line-2);
  margin-top: 20px; padding-top: 20px;
}
.mc-enroll-step { margin-bottom: 20px; }
.mc-enroll-step-num {
  display: inline-flex; align-items: center; justify-content: center;
  width: 24px; height: 24px; border-radius: 50%;
  background: var(--mc-violet); color: white;
  font-family: var(--font-mono); font-size: 11px; font-weight: 700;
  margin-bottom: 10px;
}
.mc-enroll-consent {
  display: flex; align-items: flex-start; gap: 12px;
  padding: 16px; background: var(--mc-paper);
  border-radius: var(--mc-r-md); border: 1px solid var(--mc-line);
  cursor: pointer; text-align: left; width: 100%;
  font-family: inherit; color: inherit;
}
.mc-enroll-consent.checked { border-color: var(--mc-violet); background: var(--mc-violet-mist); }
.mc-enroll-consent .checkbox {
  width: 22px; height: 22px; border-radius: 6px;
  border: 1.5px solid var(--mc-line-2); flex-shrink: 0;
  display: inline-flex; align-items: center; justify-content: center;
  transition: all 0.18s var(--ease-out);
}
.mc-enroll-consent.checked .checkbox {
  background: var(--mc-violet); border-color: var(--mc-violet); color: white;
}
.mc-enroll-consent .legal {
  font-size: 13px; color: var(--mc-ink-2); line-height: 1.5;
}
.mc-enroll-consent .legal b { color: var(--mc-black); font-weight: 600; }

.mc-enroll-photo {
  border: 2px dashed var(--mc-line-2); border-radius: var(--mc-r-md);
  padding: 32px 20px; text-align: center;
  background: var(--mc-paper); position: relative;
}
.mc-enroll-photo p { font-size: 14px; color: var(--mc-ink-2); margin: 8px 0 0; }
.mc-enroll-photo-req { font-size: 12px; color: var(--mc-ink-4); margin-top: 8px; }
.mc-enroll-preview {
  width: 100%; max-height: 200px; object-fit: cover;
  border-radius: var(--mc-r-sm); margin-bottom: 12px;
}
```

- [ ] **Step 1.3: CSS bloques Próximo Pago, Tu Espacio, Gestionar**

```css
/* ── Block 2: Próximo pago ─────────────────────────────────────── */
.mc-pago-amount {
  font-family: var(--font-cond); font-size: 36px; font-weight: 900;
  letter-spacing: -0.04em; color: var(--mc-black);
}
.mc-pago-amount small {
  font-family: var(--font-mono); font-size: 11px;
  color: var(--mc-ink-3); font-weight: 500; margin-left: 4px;
}
.mc-pago-next {
  font-size: 13px; color: var(--mc-ink-3);
  font-family: var(--font-mono); letter-spacing: 0.04em;
  margin-top: 4px; margin-bottom: 20px;
}
.mc-pago-history { border-top: 1px solid var(--mc-line-2); padding-top: 16px; }
.mc-pago-row {
  display: flex; align-items: baseline; justify-content: space-between;
  padding: 8px 0; font-size: 13px; color: var(--mc-ink-2);
  border-bottom: 1px solid var(--mc-line);
}
.mc-pago-row:last-child { border-bottom: none; }
.mc-pago-row b { font-family: var(--font-cond); color: var(--mc-black); font-weight: 700; }
.mc-pago-row a {
  font-family: var(--font-mono); font-size: 10px;
  color: var(--mc-violet); letter-spacing: 0.06em; font-weight: 600;
}
.mc-pago-row a:hover { text-decoration: underline; }
.mc-pago-alert {
  background: #fee2e2; border: 1px solid #fca5a5;
  border-radius: var(--mc-r-md); padding: 14px 16px;
  margin-bottom: 16px;
}
.mc-pago-alert b { color: #991b1b; font-size: 14px; display: block; margin-bottom: 4px; }
.mc-pago-alert p { font-size: 13px; color: #7f1d1d; margin: 0 0 12px; }

/* ── Block 3: Tu espacio ───────────────────────────────────────── */
.mc-espacio-grid {
  display: grid; grid-template-columns: 1fr 1fr; gap: 16px;
}
.mc-espacio-item { display: flex; flex-direction: column; gap: 3px; }
.mc-espacio-item .lbl {
  font-family: var(--font-mono); font-size: 10px;
  letter-spacing: 0.12em; text-transform: uppercase;
  color: var(--mc-ink-4); font-weight: 600;
}
.mc-espacio-item b { font-size: 15px; color: var(--mc-black); font-weight: 600; }
.mc-espacio-item span { font-size: 13px; color: var(--mc-ink-3); }
@media (max-width: 480px) {
  .mc-espacio-grid { grid-template-columns: 1fr; }
}

/* ── Block 4: Gestionar ────────────────────────────────────────── */
.mc-gestionar-actions {
  display: flex; flex-direction: column; gap: 10px;
}
.mc-gestionar-divider {
  border: none; border-top: 1px solid var(--mc-line);
  margin: 8px 0;
}
.mc-cancel-confirm {
  background: #fff7f7; border: 1px solid #fca5a5;
  border-radius: var(--mc-r-md); padding: 18px;
  margin-top: 12px;
}
.mc-cancel-confirm p { font-size: 14px; color: var(--mc-ink-1); margin: 0 0 6px; }
.mc-cancel-confirm .date {
  font-family: var(--font-mono); font-size: 13px;
  color: #991b1b; font-weight: 700; margin-bottom: 14px;
}
.mc-cancel-confirm-actions { display: flex; gap: 10px; }

/* ── ResizeModal ────────────────────────────────────────────────── */
.mc-resize-overlay {
  position: fixed; inset: 0; z-index: 100;
  background: rgba(10, 10, 10, 0.55);
  backdrop-filter: blur(8px); -webkit-backdrop-filter: blur(8px);
  display: flex; align-items: flex-end; justify-content: center;
  padding: 0; animation: fade 0.2s var(--ease-out);
}
@media (min-width: 601px) {
  .mc-resize-overlay { align-items: center; padding: 24px; }
}
.mc-resize-modal {
  background: var(--mc-paper); width: 100%; max-width: 560px;
  border-radius: var(--mc-r-lg) var(--mc-r-lg) 0 0;
  padding: 28px 24px 36px;
  box-shadow: 0 -20px 60px -10px rgba(10,10,10,0.3);
  animation: pop 0.35s var(--ease-spring);
  max-height: 90dvh; overflow-y: auto;
}
@media (min-width: 601px) {
  .mc-resize-modal { border-radius: var(--mc-r-lg); }
}
.mc-resize-modal h3 {
  font-family: var(--font-cond); font-size: 24px; font-weight: 900;
  letter-spacing: -0.025em; margin: 0 0 6px;
}
.mc-resize-modal .sub { font-size: 14px; color: var(--mc-ink-2); margin-bottom: 22px; }
.mc-resize-current {
  background: var(--mc-violet-mist); border: 1.5px solid var(--mc-violet);
  border-radius: var(--mc-r-md); padding: 14px 16px; margin-bottom: 16px;
  font-family: var(--font-mono); font-size: 11px; color: var(--mc-violet);
  font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase;
}
.mc-resize-opts { display: flex; flex-direction: column; gap: 8px; margin-bottom: 20px; }
.mc-resize-opt {
  display: flex; align-items: center; justify-content: space-between;
  padding: 14px 16px; background: var(--mc-card);
  border: 1.5px solid var(--mc-line-2); border-radius: var(--mc-r-md);
  cursor: pointer; width: 100%; text-align: left; font-family: inherit;
  transition: border-color 0.18s var(--ease-out), background 0.18s var(--ease-out);
}
.mc-resize-opt:hover { border-color: var(--mc-green); }
.mc-resize-opt.selected { border-color: var(--mc-green-ink); background: var(--mc-green-mist); }
.mc-resize-opt b { font-family: var(--font-cond); font-size: 18px; font-weight: 900; letter-spacing: -0.02em; }
.mc-resize-opt span { font-size: 13px; color: var(--mc-ink-3); }
.mc-resize-opt .price { font-family: var(--font-cond); font-weight: 700; color: var(--mc-green-ink); }
.mc-resize-actions { display: flex; gap: 10px; }
.mc-resize-success { text-align: center; padding: 16px 0; }
.mc-resize-success .icon { font-size: 40px; margin-bottom: 12px; }
.mc-resize-success h4 { font-family: var(--font-cond); font-size: 22px; font-weight: 900; margin: 0 0 6px; }
.mc-resize-success p { font-size: 14px; color: var(--mc-ink-2); }

/* ── Reservation Selector (2+ reservas) ─────────────────────────── */
.mc-res-selector { max-width: 680px; margin: 0 auto; padding: 0 20px 80px; }
.mc-res-selector h2 {
  font-family: var(--font-cond); font-size: 28px; font-weight: 900;
  letter-spacing: -0.025em; margin: 32px 0 20px;
}
.mc-res-selector-grid { display: flex; flex-direction: column; gap: 10px; }
.mc-res-selector-card {
  display: flex; align-items: center; justify-content: space-between;
  padding: 18px 20px; background: var(--mc-card);
  border: 1.5px solid var(--mc-line-2); border-radius: var(--mc-r-md);
  cursor: pointer; text-align: left; font-family: inherit;
  transition: border-color 0.18s var(--ease-out);
  text-decoration: none; color: inherit;
}
.mc-res-selector-card:hover { border-color: var(--mc-violet); }
.mc-res-selector-card .info b { display: block; font-family: var(--font-cond); font-size: 18px; font-weight: 900; letter-spacing: -0.015em; }
.mc-res-selector-card .info span { font-size: 13px; color: var(--mc-ink-3); }
.mc-res-selector-card .arrow { font-size: 18px; color: var(--mc-ink-4); }
```

- [ ] **Step 1.4: Verificar en browser que los estilos no rompen nada**

Abrir http://localhost:5173 — la página home debe verse igual que antes (el CSS nuevo no tiene selectors que pisen estilos existentes).

- [ ] **Step 1.5: Commit**

```bash
git add styles.css
git commit -m "feat: CSS para portal v2 (acceso facial, pagos, gestionar, resize modal)"
```

---

## Task 2: Routing update y smart entry logic

**Files:**
- Modify: `home.jsx` — función `useHashRoute` (línea 277) y routing en `App` (línea 1809)

- [ ] **Step 2.1: Actualizar `useHashRoute` para sub-rutas del portal**

Reemplazar el contenido de `useHashRoute` (líneas 277–294) con:

```javascript
function useHashRoute() {
  const parse = () => {
    const raw = window.location.hash.replace(/^#/, '').replace(/^\//, '');
    if (!raw) return { name: 'home', params: {} };
    const segs = raw.split('/').filter(Boolean);
    if (segs[0] === 'portal' && segs[1] === 'r' && segs[2] && segs[3] === 'acceso')
      return { name: 'reservation', params: { id: segs[2], view: 'acceso' } };
    if (segs[0] === 'portal' && segs[1] === 'r' && segs[2] && segs[3] === 'cambiar')
      return { name: 'reservation', params: { id: segs[2], view: 'cambiar' } };
    if (segs[0] === 'portal' && segs[1] === 'r' && segs[2])
      return { name: 'reservation', params: { id: segs[2], view: null } };
    if (segs[0] === 'portal') return { name: 'portal', params: {} };
    if (segs[0] === 'reservar') return { name: 'home', params: {}, openWizard: true };
    return { name: 'home', params: {} };
  };
  const [route, setRoute] = useState(parse);
  useEffect(() => {
    const onChange = () => setRoute(parse());
    window.addEventListener('hashchange', onChange);
    return () => window.removeEventListener('hashchange', onChange);
  }, []);
  return route;
}
```

- [ ] **Step 2.2: Actualizar el routing en App para Option C (0/1/2+ reservas)**

Reemplazar el bloque del portal en el render de `App` (líneas 1833–1849). Encontrar:

```javascript
      {route.name === 'portal' && (
        <main id="main" className="mc-portal-bg">
          {user
            ? <PortalDashboard user={user} reservations={userReservations} onLogout={() => store.setUser(null)} onReserve={() => openWizard()} />
            : <PortalLogin />
          }
        </main>
      )}

      {route.name === 'reservation' && (
        <main id="main" className="mc-portal-bg">
          {user
            ? <ReservationDetail reservation={userReservations.find((r) => r.id === route.params.id)} user={user} onUpdate={store.updateReservation} />
            : <PortalLogin />
          }
        </main>
      )}
```

Reemplazar con:

```javascript
      {(route.name === 'portal' || route.name === 'reservation') && (
        <main id="main" className="mc-portal-bg">
          {!user ? (
            <PortalLogin />
          ) : route.name === 'portal' ? (
            <PortalEntry
              user={user}
              reservations={userReservations}
              onLogout={() => store.setUser(null)}
              onReserve={() => openWizard()}
            />
          ) : (
            <ReservationPortal
              reservation={userReservations.find((r) => r.id === route.params.id)}
              user={user}
              initialView={route.params.view}
              onUpdate={store.updateReservation}
              allCategories={CATEGORIES}
            />
          )}
        </main>
      )}
```

- [ ] **Step 2.3: Agregar componente `PortalEntry` (smart entry — 0/1/2+)**

Agregar después de la función `PortalDashboard` existente (después de la línea 1679):

```javascript
// PortalEntry: decide qué mostrar según cantidad de reservas
function PortalEntry({ user, reservations, onLogout, onReserve }) {
  const active = reservations.filter((r) => r.status !== 'cancelled');

  // Redirigir directo si hay exactamente 1 reserva
  useEffect(() => {
    if (active.length === 1) {
      window.location.hash = `#/portal/r/${active[0].id}`;
    }
  }, []);

  // 0 reservas — empty state
  if (reservations.length === 0) {
    return (
      <>
        <section className="mc-portal-hero">
          <div className="mc-portal-hero-inner">
            <span className="mc-eyebrow on-dark">Portal cliente</span>
            <h1>Hola, <span className="g">{user.name || user.email.split('@')[0]}</span>.</h1>
          </div>
        </section>
        <div className="mc-res-selector">
          <div className="mc-portal-block" style={{ textAlign: 'center', padding: '48px 24px' }}>
            <div style={{ fontSize: 40, marginBottom: 12 }}>📦</div>
            <h3 style={{ fontFamily: 'var(--font-cond)', fontSize: 22, fontWeight: 900, margin: '0 0 8px' }}>
              Todavía no tenés espacios
            </h3>
            <p style={{ fontSize: 14, color: 'var(--mc-ink-2)', marginBottom: 24 }}>
              Reservá tu primer espacio en menos de 5 minutos.<br />
              Sin depósito · 1° mes gratis.
            </p>
            <button className="mc-btn mc-btn-green" onClick={onReserve}>
              <span>Reservar ahora</span><span className="arrow">→</span>
            </button>
          </div>
          <div style={{ marginTop: 16 }}>
            <button className="mc-btn mc-btn-ghost" onClick={onLogout}><span>Cerrar sesión</span></button>
          </div>
        </div>
      </>
    );
  }

  // 1 reserva — mostrando spinner mientras redirige
  if (active.length === 1) {
    return (
      <div style={{ display: 'flex', justifyContent: 'center', padding: '80px 20px' }}>
        <div style={{ fontFamily: 'var(--font-mono)', fontSize: 12, color: 'var(--mc-ink-4)' }}>
          Cargando tu espacio...
        </div>
      </div>
    );
  }

  // 2+ reservas — selector
  return (
    <>
      <section className="mc-portal-hero">
        <div className="mc-portal-hero-inner">
          <span className="mc-eyebrow on-dark">Portal cliente</span>
          <h1>Hola, <span className="g">{user.name || user.email.split('@')[0]}</span>.</h1>
          <p>Elegí el espacio que querés gestionar.</p>
        </div>
      </section>
      <div className="mc-res-selector">
        <h2>Tus espacios</h2>
        <div className="mc-res-selector-grid">
          {reservations.map((r) => {
            const statusMap = {
              active: 'Activa', pending_payment: 'Pendiente de pago',
              cancelled: 'Cancelada', payment_failed: 'Pago fallido',
              cancellation_scheduled: 'Cancelación programada',
            };
            return (
              <a key={r.id} className="mc-res-selector-card" href={`#/portal/r/${r.id}`}>
                <div className="info">
                  <b>{r.category?.label || r.size?.label} · {r.option ? `${formatM2(r.option.m2)} m²` : r.size?.range}</b>
                  <span>{r.sucursal?.name} · {statusMap[r.status] || r.status}</span>
                </div>
                <span className="arrow">→</span>
              </a>
            );
          })}
          <button className="mc-btn mc-btn-ghost" onClick={onReserve} style={{ marginTop: 8 }}>
            <span>+ Nueva reserva</span>
          </button>
        </div>
        <div style={{ marginTop: 24 }}>
          <button className="mc-btn mc-btn-ghost" onClick={onLogout}><span>Cerrar sesión</span></button>
        </div>
      </div>
    </>
  );
}
```

- [ ] **Step 2.4: Verificar en browser**

1. Abrir http://localhost:5173/#/portal
2. Login con cualquier email
3. Con 0 reservas → debe mostrar empty state con "Reservar ahora"
4. Reservar un espacio (wizard)
5. Volver a `#/portal` → debe redirigir automáticamente a `#/portal/r/{id}`
6. Hacer una segunda reserva → volver a `#/portal` → debe mostrar el selector con 2 tarjetas

- [ ] **Step 2.5: Commit**

```bash
git add home.jsx
git commit -m "feat: portal entry logic — 0/1/2+ reservas, smart redirect"
```

---

## Task 3: Header de reserva con estados y `ReservationPortal` shell

**Files:**
- Modify: `home.jsx` — agregar `ReservationPortal` (reemplaza `ReservationDetail`)

- [ ] **Step 3.1: Agregar `ReservationPortal` con header y 4 bloques vacíos**

Agregar después de `PortalEntry` (que agregamos en Task 2). Este será el componente que iremos completando en las siguientes tasks:

```javascript
function ReservationPortal({ reservation, user, initialView, onUpdate, allCategories }) {
  const [resizeOpen, setResizeOpen] = useState(initialView === 'cambiar');
  const [cancelStep, setCancelStep] = useState(0); // 0=hidden, 1=confirm, 2=done

  if (!reservation) {
    return (
      <div className="mc-res-portal">
        <div className="mc-portal-block" style={{ textAlign: 'center', padding: 56 }}>
          <h3>Reserva no encontrada</h3>
          <p style={{ fontSize: 14, color: 'var(--mc-ink-2)', marginTop: 8 }}>
            El código no coincide con ninguna reserva tuya.
          </p>
          <a className="mc-btn mc-btn-violet" href="#/portal" style={{ marginTop: 18, display: 'inline-flex' }}>
            <span>← Volver</span>
          </a>
        </div>
      </div>
    );
  }

  const catLabel = reservation.category?.label || reservation.size?.label || '—';
  const m2Label = reservation.option ? `${formatM2(reservation.option.m2)} m²` : reservation.size?.range || '—';

  const statusConfig = {
    active:                  { label: 'Activa',                   cls: 'active' },
    pending_payment:         { label: 'Esperando pago',            cls: 'pending' },
    payment_failed:          { label: 'Pago fallido',              cls: 'failed' },
    cancellation_scheduled:  { label: `Cancela el ${reservation.cancelEffectiveDate || '—'}`, cls: 'scheduled' },
    cancelled:               { label: 'Cancelada',                 cls: 'cancelled' },
  };
  const sc = statusConfig[reservation.status] || { label: reservation.status, cls: 'pending' };

  return (
    <>
      <section className="mc-portal-hero" style={{ paddingBottom: 0 }}>
        <div className="mc-portal-hero-inner" style={{ paddingBottom: 24 }}>
          <a className="mc-res-portal-back" href="#/portal">← Tus espacios</a>
          <div className="mc-res-portal-header">
            <div>
              <h1>{catLabel} <span className="g">· {reservation.sucursal?.name}</span></h1>
              <div style={{ fontFamily: 'var(--font-mono)', fontSize: 11, color: 'rgba(255,255,255,0.5)', letterSpacing: '0.06em' }}>
                {reservation.id}
              </div>
            </div>
            <span className={`mc-portal-status ${sc.cls}`}>{sc.label}</span>
          </div>
        </div>
      </section>

      <div className="mc-res-portal">
        {/* Block 1 — Acceso facial */}
        <AccesoFacial
          reservation={reservation}
          onUpdate={onUpdate}
        />

        {/* Block 2 — Próximo pago */}
        <ProximoPago reservation={reservation} />

        {/* Block 3 — Tu espacio */}
        <TuEspacio reservation={reservation} m2Label={m2Label} catLabel={catLabel} />

        {/* Block 4 — Gestionar */}
        <Gestionar
          reservation={reservation}
          onUpdate={onUpdate}
          onOpenResize={() => setResizeOpen(true)}
          cancelStep={cancelStep}
          setCancelStep={setCancelStep}
        />
      </div>

      {/* Resize modal (portal level, not inside a block) */}
      {resizeOpen && (
        <ResizeModal
          reservation={reservation}
          allCategories={allCategories}
          onClose={() => setResizeOpen(false)}
          onUpdate={onUpdate}
        />
      )}
    </>
  );
}
```

- [ ] **Step 3.2: Agregar stubs de los 4 bloques (para que compile)**

Agregar los stubs justo antes de `ReservationPortal`:

```javascript
// Stubs — se reemplazan en tasks siguientes
function AccesoFacial({ reservation, onUpdate }) {
  return (
    <div className="mc-portal-block mc-acceso">
      <span className="mc-portal-block-title">Acceso facial</span>
      <p style={{ fontSize: 14, color: 'var(--mc-ink-2)' }}>Cargando...</p>
    </div>
  );
}
function ProximoPago({ reservation }) {
  return (
    <div className="mc-portal-block mc-pago">
      <span className="mc-portal-block-title">Próximo pago</span>
      <p style={{ fontSize: 14, color: 'var(--mc-ink-2)' }}>Cargando...</p>
    </div>
  );
}
function TuEspacio({ reservation, m2Label, catLabel }) {
  return (
    <div className="mc-portal-block mc-espacio">
      <span className="mc-portal-block-title">Tu espacio</span>
      <p style={{ fontSize: 14, color: 'var(--mc-ink-2)' }}>Cargando...</p>
    </div>
  );
}
function Gestionar({ reservation, onUpdate, onOpenResize, cancelStep, setCancelStep }) {
  return (
    <div className="mc-portal-block mc-gestionar">
      <span className="mc-portal-block-title">Gestionar</span>
      <p style={{ fontSize: 14, color: 'var(--mc-ink-2)' }}>Cargando...</p>
    </div>
  );
}
function ResizeModal({ reservation, allCategories, onClose, onUpdate }) {
  return null;
}
```

- [ ] **Step 3.3: Verificar en browser**

1. http://localhost:5173/#/portal → login → reserva existente → `#/portal/r/{id}`
2. Debe mostrar header con nombre, m², badge de estado
3. Cuatro bloques placeholder visibles
4. El back link `← Tus espacios` navega a `#/portal`

- [ ] **Step 3.4: Commit**

```bash
git add home.jsx
git commit -m "feat: ReservationPortal shell con header + 4 bloques stub"
```

---

## Task 4: Bloque Acceso Facial (4 estados)

**Files:**
- Modify: `home.jsx` — reemplazar el stub `AccesoFacial`

- [ ] **Step 4.1: Agregar campo `faceEnrollStatus` al store**

El campo `faceEnrollStatus` ya puede existir en la reserva desde el wizard. Para las reservas antiguas sin ese campo, el default es `'not_started'`. No hay que cambiar el store — se usa `reservation.faceEnrollStatus || 'not_started'`.

- [ ] **Step 4.2: Reemplazar el stub `AccesoFacial` con la implementación completa**

Reemplazar la función `AccesoFacial` con:

```javascript
function AccesoFacial({ reservation, onUpdate }) {
  const [enrolling, setEnrolling] = useState(false);
  const status = reservation.faceEnrollStatus || 'not_started';

  // Estado C — activo
  if (status === 'enrolled') {
    return (
      <div className="mc-portal-block mc-acceso state-active">
        <span className="mc-portal-block-title">Acceso facial</span>
        <div className="mc-acceso-icon check">✅</div>
        <h3>Acceso activo — {reservation.sucursal?.name}</h3>
        <p>Podás entrar las 24 hs. Tu cara es tu credencial.</p>
        <div className="sub">{reservation.sucursal?.hours || 'Horarios de atención según sucursal'}</div>
      </div>
    );
  }

  // Estado B — procesando
  if (status === 'queued') {
    return (
      <div className="mc-portal-block mc-acceso state-processing">
        <span className="mc-portal-block-title">Acceso facial</span>
        <div className="mc-acceso-icon spin">⚙️</div>
        <h3>Activando tu acceso...</h3>
        <p>Estamos registrando tu cara en {reservation.sucursal?.name}. En minutos recibís un email.</p>
      </div>
    );
  }

  // Estado D — error
  if (status === 'failed') {
    return (
      <div className="mc-portal-block mc-acceso state-failed">
        <span className="mc-portal-block-title">Acceso facial</span>
        <div className="mc-acceso-icon warning">⚠️</div>
        <h3>Error al activar el acceso</h3>
        <p>La foto no cumplió los requisitos mínimos.</p>
        <div className="mc-acceso-actions">
          <button className="mc-btn mc-btn-violet" onClick={() => setEnrolling(true)}>
            <span>Intentar de nuevo</span>
          </button>
          <a className="mc-btn mc-btn-ghost" href={`https://wa.me/5491136207989?text=Hola,%20necesito%20ayuda%20con%20el%20acceso%20facial%20de%20mi%20reserva%20${reservation.id}`} target="_blank" rel="noreferrer">
            <span>Contactar por WhatsApp</span>
          </a>
        </div>
        {enrolling && <FaceEnrollFlow reservation={reservation} onUpdate={onUpdate} onDone={() => setEnrolling(false)} />}
      </div>
    );
  }

  // Estado A — not_started (default)
  return (
    <div className="mc-portal-block mc-acceso">
      <span className="mc-portal-block-title">Acceso facial</span>
      {!enrolling ? (
        <>
          <div className="mc-acceso-icon lock">🔒</div>
          <h3>Activá tu acceso al local</h3>
          <p>
            Tu espacio está listo. Solo falta registrar tu cara para poder entrar.
            {reservation.status === 'pending_payment' && ' (Disponible una vez confirmado el pago.)'}
          </p>
          {reservation.status === 'active' && (
            <button className="mc-btn mc-btn-violet" onClick={() => setEnrolling(true)}>
              <span>Activar acceso →</span>
            </button>
          )}
          <div className="sub">Solo necesitás una foto · tarda 2 minutos</div>
        </>
      ) : (
        <FaceEnrollFlow reservation={reservation} onUpdate={onUpdate} onDone={() => setEnrolling(false)} />
      )}
    </div>
  );
}
```

- [ ] **Step 4.3: Verificar los 4 estados en browser**

Usar la consola del browser para forzar cada estado:

```javascript
// Abrir http://localhost:5173/#/portal/r/{tu-reserva-id}
// En la consola:

// Estado A (default): no hacer nada

// Estado B (procesando):
const rs = JSON.parse(localStorage.getItem('mc.reservations'));
rs[0].faceEnrollStatus = 'queued';
localStorage.setItem('mc.reservations', JSON.stringify(rs));
location.reload();

// Estado C (activo):
rs[0].faceEnrollStatus = 'enrolled';
localStorage.setItem('mc.reservations', JSON.stringify(rs));
location.reload();

// Estado D (error):
rs[0].faceEnrollStatus = 'failed';
localStorage.setItem('mc.reservations', JSON.stringify(rs));
location.reload();
```

Cada estado debe mostrar el bloque correcto.

- [ ] **Step 4.4: Commit**

```bash
git add home.jsx
git commit -m "feat: bloque Acceso Facial — 4 estados (not_started, queued, enrolled, failed)"
```

---

## Task 5: FaceEnrollFlow — flujo de 3 pasos inline

**Files:**
- Modify: `home.jsx` — agregar `FaceEnrollFlow` antes de `AccesoFacial`

- [ ] **Step 5.1: Agregar `FaceEnrollFlow`**

```javascript
function FaceEnrollFlow({ reservation, onUpdate, onDone }) {
  const [step, setStep] = useState(1); // 1=consent, 2=photo, 3=sent
  const [consented, setConsented] = useState(false);
  const [photoPreview, setPhotoPreview] = useState(null);
  const fileRef = useRef(null);

  const handleFile = (e) => {
    const file = e.target.files[0];
    if (!file) return;
    const reader = new FileReader();
    reader.onload = (ev) => setPhotoPreview(ev.target.result);
    reader.readAsDataURL(file);
  };

  const handleSend = () => {
    onUpdate(reservation.id, { faceEnrollStatus: 'queued' });
    setStep(3);
  };

  const handleDone = () => {
    onDone();
  };

  return (
    <div className="mc-enroll">
      {/* Paso 1 — Consentimiento */}
      {step === 1 && (
        <div className="mc-enroll-step">
          <div className="mc-enroll-step-num">1</div>
          <p style={{ fontSize: 14, color: 'var(--mc-ink-1)', fontWeight: 600, marginBottom: 12 }}>
            Consentimiento biométrico (Ley 25.326)
          </p>
          <button
            className={`mc-enroll-consent ${consented ? 'checked' : ''}`}
            onClick={() => setConsented(!consented)}
            type="button"
          >
            <span className="checkbox">
              {consented && <svg width="12" height="9" viewBox="0 0 12 9" fill="none"><path d="M1 4L4.5 7.5L11 1" stroke="white" strokeWidth="1.8" strokeLinecap="round" strokeLinejoin="round"/></svg>}
            </span>
            <span className="legal">
              <b>Acepto el uso de mis datos biométricos</b> exclusivamente para el control de acceso al local de Mi Container (CORDIS MS SA). Mi foto se usa para registrar mi cara en el sistema Hikvision del local y se elimina de los servidores dentro de las 24 hs. El dato biométrico reside únicamente en el dispositivo de acceso.
            </span>
          </button>
          <button
            className="mc-btn mc-btn-violet"
            style={{ marginTop: 14 }}
            disabled={!consented}
            onClick={() => setStep(2)}
          >
            <span>Continuar →</span>
          </button>
          <button className="mc-btn mc-btn-ghost" style={{ marginTop: 8 }} onClick={onDone}>
            <span>Cancelar</span>
          </button>
        </div>
      )}

      {/* Paso 2 — Foto */}
      {step === 2 && (
        <div className="mc-enroll-step">
          <div className="mc-enroll-step-num">2</div>
          <p style={{ fontSize: 14, color: 'var(--mc-ink-1)', fontWeight: 600, marginBottom: 12 }}>
            Sacate una foto o subí una imagen
          </p>
          <div className="mc-enroll-photo" onClick={() => fileRef.current?.click()} style={{ cursor: 'pointer' }}>
            {photoPreview
              ? <img src={photoPreview} alt="Vista previa" className="mc-enroll-preview" />
              : <span style={{ fontSize: 28 }}>📷</span>
            }
            <p>{photoPreview ? 'Tocá para cambiar' : 'Tocá para elegir foto'}</p>
            <div className="mc-enroll-photo-req">
              Cara centrada · buena iluminación · sin anteojos de sol · mínimo 640×480px
            </div>
          </div>
          <input
            ref={fileRef}
            type="file"
            accept="image/*"
            capture="user"
            onChange={handleFile}
            style={{ display: 'none' }}
          />
          <div style={{ display: 'flex', gap: 10, marginTop: 14 }}>
            <button
              className="mc-btn mc-btn-violet"
              disabled={!photoPreview}
              onClick={handleSend}
            >
              <span>Enviar foto →</span>
            </button>
            <button className="mc-btn mc-btn-ghost" onClick={() => setStep(1)}>
              <span>← Atrás</span>
            </button>
          </div>
        </div>
      )}

      {/* Paso 3 — Enviado */}
      {step === 3 && (
        <div className="mc-enroll-step" style={{ textAlign: 'center', paddingTop: 8 }}>
          <div style={{ fontSize: 36, marginBottom: 12 }}>⏳</div>
          <p style={{ fontSize: 15, fontWeight: 600, color: 'var(--mc-ink-1)', marginBottom: 6 }}>
            Activando tu acceso...
          </p>
          <p style={{ fontSize: 14, color: 'var(--mc-ink-2)', marginBottom: 20 }}>
            Estamos registrando tu cara en {reservation.sucursal?.name}.<br />
            Te avisamos por email cuando tu acceso esté listo.
          </p>
          <button className="mc-btn mc-btn-ghost" onClick={handleDone}>
            <span>Entendido</span>
          </button>
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 5.2: Verificar el flujo completo**

1. En browser, ir a la reserva con `faceEnrollStatus: 'not_started'`
2. Click "Activar acceso" → debe mostrar Paso 1 (consentimiento)
3. Checkbox desactivado → botón "Continuar" disabled
4. Activar checkbox → botón habilitado → click Continuar
5. Paso 2: área de foto, click → abre file picker → seleccionar imagen → preview
6. "Enviar foto" → Paso 3 (spinner + mensaje)
7. "Entendido" → cierra el flow → el bloque ahora muestra Estado B (queued)

- [ ] **Step 5.3: Commit**

```bash
git add home.jsx
git commit -m "feat: FaceEnrollFlow — 3 pasos inline (consentimiento, foto, enviado)"
```

---

## Task 6: Bloque Próximo Pago

**Files:**
- Modify: `home.jsx` — reemplazar stub `ProximoPago`

- [ ] **Step 6.1: Reemplazar stub `ProximoPago`**

```javascript
function ProximoPago({ reservation }) {
  // Calcular próxima fecha de cobro (startDate + 1 mes, o hoy + 1 mes si ya pasó)
  const calcNextPayment = () => {
    try {
      const start = new Date(reservation.startDate);
      const today = new Date();
      const next = new Date(start);
      while (next <= today) next.setMonth(next.getMonth() + 1);
      return next.toLocaleDateString('es-AR', { day: 'numeric', month: 'long', year: 'numeric' });
    } catch { return '—'; }
  };

  const nextDate = calcNextPayment();
  const monthly = reservation.monthly || 0;
  const isFailed = reservation.status === 'payment_failed';

  // Historial de pagos simulado (en producción viene de /payments de Firestore)
  const mockHistory = (() => {
    if (!reservation.startDate) return [];
    const rows = [];
    try {
      const start = new Date(reservation.startDate);
      const today = new Date();
      const d = new Date(start);
      let isFirst = true;
      while (d < today) {
        const label = d.toLocaleDateString('es-AR', { month: 'short', year: 'numeric' });
        const amount = isFirst ? reservation.firstMonth : monthly;
        const promoLabel = isFirst && reservation.promosApplied?.some((p) => p.key === 'first-month-free')
          ? '1° mes gratis' : null;
        rows.push({ label, amount, promoLabel });
        d.setMonth(d.getMonth() + 1);
        isFirst = false;
      }
    } catch { return []; }
    return rows.reverse().slice(0, 4); // últimos 4 meses
  })();

  return (
    <div className="mc-portal-block mc-pago">
      <span className="mc-portal-block-title">Próximo pago</span>

      {isFailed && (
        <div className="mc-pago-alert">
          <b>Problema con tu pago</b>
          <p>No pudimos cobrar el monto mensual. Tu acceso se suspende en 7 días si no se actualiza la tarjeta.</p>
          <a
            className="mc-btn mc-btn-ghost"
            href="https://www.mercadopago.com.ar/subscriptions"
            target="_blank"
            rel="noreferrer"
            style={{ fontSize: 13 }}
          >
            <span>Actualizar tarjeta en Mercado Pago →</span>
          </a>
        </div>
      )}

      {!isFailed && (
        <>
          <div className="mc-pago-amount">
            ${monthly.toLocaleString('es-AR')}
            <small>/ mes</small>
          </div>
          <div className="mc-pago-next">Próximo cobro: {nextDate}</div>
        </>
      )}

      {mockHistory.length > 0 && (
        <div className="mc-pago-history">
          <div style={{ fontFamily: 'var(--font-mono)', fontSize: 10, letterSpacing: '0.1em', textTransform: 'uppercase', color: 'var(--mc-ink-4)', marginBottom: 10 }}>
            Historial
          </div>
          {mockHistory.map((row, i) => (
            <div key={i} className="mc-pago-row">
              <span>{row.label}{row.promoLabel && <span style={{ marginLeft: 6, color: 'var(--mc-green-ink)', fontFamily: 'var(--font-mono)', fontSize: 10 }}>{row.promoLabel}</span>}</span>
              <div style={{ display: 'flex', alignItems: 'center', gap: 12 }}>
                <b>${row.amount.toLocaleString('es-AR')}</b>
                <a href="#" onClick={(e) => e.preventDefault()}>ver factura ↗</a>
              </div>
            </div>
          ))}
        </div>
      )}
    </div>
  );
}
```

- [ ] **Step 6.2: Verificar**

1. Con reserva activa: debe mostrar monto + próxima fecha + historial
2. Forzar `payment_failed` en localStorage → debe mostrar banner rojo con link a MP

- [ ] **Step 6.3: Commit**

```bash
git add home.jsx
git commit -m "feat: bloque Próximo Pago con historial y estado de pago fallido"
```

---

## Task 7: Bloque Tu Espacio

**Files:**
- Modify: `home.jsx` — reemplazar stub `TuEspacio`

- [ ] **Step 7.1: Reemplazar stub `TuEspacio`**

```javascript
function TuEspacio({ reservation, m2Label, catLabel }) {
  const addonNames = (reservation.addons || [])
    .map((k) => ADDONS.find((a) => a.key === k)?.name)
    .filter(Boolean);

  return (
    <div className="mc-portal-block mc-espacio">
      <span className="mc-portal-block-title">Tu espacio</span>
      <div className="mc-espacio-grid">
        <div className="mc-espacio-item">
          <span className="lbl">Espacio</span>
          <b>{catLabel} · {m2Label}</b>
        </div>
        <div className="mc-espacio-item">
          <span className="lbl">Sucursal</span>
          <b>{reservation.sucursal?.name}</b>
          <span>{reservation.sucursal?.address}</span>
        </div>
        <div className="mc-espacio-item">
          <span className="lbl">Horarios de atención</span>
          <span>{reservation.sucursal?.hours || 'Consultar'}</span>
        </div>
        <div className="mc-espacio-item">
          <span className="lbl">Inicio</span>
          <span>{reservation.startDate}</span>
        </div>
        {addonNames.length > 0 && (
          <div className="mc-espacio-item" style={{ gridColumn: '1 / -1' }}>
            <span className="lbl">Add-ons</span>
            <span>{addonNames.join(' · ')}</span>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 7.2: Verificar**

Grid 2 columnas con datos limpios. En mobile (≤480px) colapsa a 1 columna (ya cubierto por el CSS en Task 1).

- [ ] **Step 7.3: Commit**

```bash
git add home.jsx
git commit -m "feat: bloque Tu Espacio — datos limpios de sucursal y espacio"
```

---

## Task 8: Bloque Gestionar + flujo Cancelar

**Files:**
- Modify: `home.jsx` — reemplazar stub `Gestionar`

- [ ] **Step 8.1: Agregar helper para calcular fecha de cancelación**

Agregar cerca de las utilidades (antes de los componentes, alrededor de la línea 100):

```javascript
const addDays = (days) => {
  const d = new Date();
  d.setDate(d.getDate() + days);
  return d.toLocaleDateString('es-AR', { day: 'numeric', month: 'long', year: 'numeric' });
};
```

- [ ] **Step 8.2: Reemplazar stub `Gestionar`**

```javascript
function Gestionar({ reservation, onUpdate, onOpenResize, cancelStep, setCancelStep }) {
  const cancelDate = addDays(7);

  const handleConfirmCancel = () => {
    onUpdate(reservation.id, {
      status: 'cancellation_scheduled',
      cancelEffectiveDate: cancelDate,
      cancelledAt: new Date().toISOString(),
    });
    setCancelStep(2);
  };

  const isActive = reservation.status === 'active';
  const isCancelScheduled = reservation.status === 'cancellation_scheduled';

  return (
    <div className="mc-portal-block mc-gestionar">
      <span className="mc-portal-block-title">Gestionar</span>

      <div className="mc-gestionar-actions">
        {/* Cambiar de tamaño */}
        {isActive && (
          <button className="mc-btn mc-btn-ghost-violet" onClick={onOpenResize}>
            <span>Cambiar de tamaño</span>
          </button>
        )}

        <hr className="mc-gestionar-divider" />

        {/* Cancelar — solo si activa */}
        {isActive && cancelStep === 0 && (
          <button
            className="mc-btn mc-btn-ghost"
            style={{ color: 'var(--mc-ink-3)', fontSize: 14 }}
            onClick={() => setCancelStep(1)}
          >
            <span>Cancelar reserva</span>
          </button>
        )}

        {/* Confirmación de cancelación */}
        {cancelStep === 1 && (
          <div className="mc-cancel-confirm">
            <p><strong>¿Cancelar esta reserva?</strong></p>
            <div className="date">Fecha efectiva: {cancelDate}</div>
            <p style={{ marginBottom: 14, color: 'var(--mc-ink-2)' }}>
              Tu acceso se desactiva en esa fecha. No se cobra más a partir del próximo ciclo.
            </p>
            <div className="mc-cancel-confirm-actions">
              <button
                className="mc-btn mc-btn-ghost"
                style={{ background: '#fee2e2', borderColor: '#fca5a5', color: '#991b1b' }}
                onClick={handleConfirmCancel}
              >
                <span>Cancelar definitivamente</span>
              </button>
              <button className="mc-btn mc-btn-ghost" onClick={() => setCancelStep(0)}>
                <span>Volver</span>
              </button>
            </div>
          </div>
        )}

        {/* Cancelación confirmada */}
        {cancelStep === 2 && (
          <div style={{ padding: '12px 0', textAlign: 'center' }}>
            <p style={{ fontSize: 14, color: 'var(--mc-ink-2)' }}>
              Cancelación programada para el <strong>{cancelDate}</strong>.<br />
              Recibís un email de confirmación.
            </p>
          </div>
        )}

        {/* Estado ya programado */}
        {isCancelScheduled && cancelStep === 0 && (
          <p style={{ fontSize: 14, color: 'var(--mc-ink-3)' }}>
            Tu reserva se cancela el <strong>{reservation.cancelEffectiveDate}</strong>.
          </p>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 8.3: Verificar flujo de cancelación**

1. Con reserva `status: 'active'`, ir a la página de la reserva
2. Click "Cancelar reserva" → debe aparecer el bloque de confirmación con fecha
3. Click "Volver" → desaparece la confirmación
4. Click "Cancelar reserva" → "Cancelar definitivamente" → estado cambia a `cancellation_scheduled`
5. El header badge debe cambiar a "Cancela el {fecha}"
6. El bloque Gestionar muestra la fecha de cancelación en texto, sin botón de cancelar

- [ ] **Step 8.4: Commit**

```bash
git add home.jsx
git commit -m "feat: bloque Gestionar + flujo de cancelación en 2 pasos"
```

---

## Task 9: ResizeModal

**Files:**
- Modify: `home.jsx` — reemplazar stub `ResizeModal`

- [ ] **Step 9.1: Reemplazar stub `ResizeModal`**

```javascript
function ResizeModal({ reservation, allCategories, onClose, onUpdate }) {
  const [selected, setSelected] = useState(null);
  const [sent, setSent] = useState(false);

  // Obtener la categoría actual de la reserva
  const currentCat = allCategories.find((c) => c.key === reservation.category?.key);
  const currentM2 = reservation.option?.m2;

  // Todas las opciones disponibles (misma categoría + otras), excepto la actual
  const allOpts = allCategories.flatMap((cat) =>
    cat.options.map((opt) => ({ ...opt, catKey: cat.key, catLabel: cat.label }))
  ).filter((opt) => opt.m2 !== currentM2);

  const handleSend = () => {
    // Guardar solicitud de cambio de tamaño en localStorage
    const request = {
      id: `RES-${Date.now()}`,
      reservationId: reservation.id,
      currentM2,
      requestedM2: selected.m2,
      requestedMonthly: selected.monthly,
      requestedCat: selected.catLabel,
      status: 'pending',
      createdAt: new Date().toISOString(),
    };
    try {
      const existing = JSON.parse(localStorage.getItem('mc.resize_requests') || '[]');
      existing.unshift(request);
      localStorage.setItem('mc.resize_requests', JSON.stringify(existing));
    } catch { /* silent */ }
    setSent(true);
  };

  return (
    <div className="mc-resize-overlay" onClick={(e) => { if (e.target === e.currentTarget) onClose(); }}>
      <div className="mc-resize-modal">
        {!sent ? (
          <>
            <h3>Cambiar de tamaño</h3>
            <div className="sub">
              Seleccioná el nuevo tamaño. El cambio se aplica en el próximo ciclo de facturación una vez confirmado.
            </div>

            <div className="mc-resize-current">
              Tamaño actual: {reservation.category?.label} · {currentM2 ? `${formatM2(currentM2)} m²` : '—'} — ${(reservation.monthly || 0).toLocaleString('es-AR')}/mes
            </div>

            <div className="mc-resize-opts">
              {allOpts.map((opt, i) => (
                <button
                  key={i}
                  className={`mc-resize-opt ${selected?.m2 === opt.m2 ? 'selected' : ''}`}
                  onClick={() => setSelected(opt)}
                >
                  <div>
                    <b>{opt.catLabel} · {formatM2(opt.m2)} m²</b>
                    <span style={{ display: 'block', fontSize: 12, marginTop: 2 }}>
                      {opt.m2 > (currentM2 || 0) ? '↑ Más espacio' : '↓ Menos espacio'}
                    </span>
                  </div>
                  <span className="price">${opt.monthly.toLocaleString('es-AR')}/mes</span>
                </button>
              ))}
            </div>

            <div className="mc-resize-actions">
              <button
                className="mc-btn mc-btn-violet"
                disabled={!selected}
                onClick={handleSend}
              >
                <span>Solicitar cambio →</span>
              </button>
              <button className="mc-btn mc-btn-ghost" onClick={onClose}>
                <span>Cancelar</span>
              </button>
            </div>
          </>
        ) : (
          <div className="mc-resize-success">
            <div className="icon">📬</div>
            <h4>Solicitud enviada</h4>
            <p>
              Te avisamos por email en 24 hs hábiles cuando el cambio esté confirmado.
              <br />Nuevo tamaño solicitado: <strong>{selected?.catLabel} · {formatM2(selected?.m2)} m²</strong>
            </p>
            <button className="mc-btn mc-btn-ghost" onClick={onClose} style={{ marginTop: 20 }}>
              <span>Cerrar</span>
            </button>
          </div>
        )}
      </div>
    </div>
  );
}
```

- [ ] **Step 9.2: Verificar el flujo de cambio de tamaño**

1. Con reserva activa, click "Cambiar de tamaño"
2. Modal abre desde abajo (bottom sheet en mobile, centrado en desktop)
3. Se ve el tamaño actual resaltado
4. Lista de opciones disponibles con precios
5. Seleccionar uno → botón "Solicitar cambio" habilitado
6. Click → pantalla de éxito con nombre del tamaño elegido
7. Click fuera del modal (overlay) → cierra
8. Verificar en `localStorage.getItem('mc.resize_requests')` que se guardó la solicitud

- [ ] **Step 9.3: Commit**

```bash
git add home.jsx
git commit -m "feat: ResizeModal — selector de tamaño con confirmación de solicitud"
```

---

## Task 10: Limpieza — remover código viejo y verificación final

**Files:**
- Modify: `home.jsx` — remover `ReservationDetail` y el botón "Pausar"

- [ ] **Step 10.1: Remover `ReservationDetail`**

El componente `ReservationDetail` (líneas ~1681–1765) ya no se usa — fue reemplazado por `ReservationPortal`. Buscar y eliminar la función completa.

También en el routing de `App`, el nuevo código ya no referencia `ReservationDetail`. Confirmar que no queda ningún uso de `ReservationDetail` en el archivo:

```bash
grep -n "ReservationDetail" home.jsx
```

Esperado: sin resultados (o solo el que eliminaste).

- [ ] **Step 10.2: Remover el QR del detail viejo**

En la lista de reservas del `PortalDashboard` existente, el campo `code` del detail mostraba el QR. Verificar que no queda lógica de QR en el portal (la línea `qrUrl = https://api.qrserver.com...`). Si queda, eliminarla.

- [ ] **Step 10.3: Verificar flujo completo end-to-end**

Secuencia completa sin interrupciones:

1. Abrir http://localhost:5173 en mobile (375px)
2. Click "Reservar" → wizard → completar los 5 pasos → reservar
3. `#/portal` → login → redirige a `#/portal/r/{id}`
4. **Header:** badge "Esperando pago" (si status = pending_payment)
5. Forzar `status: 'active'` en localStorage → reload → badge verde "Activa"
6. **Block 1:** "Activar acceso" → consentimiento → foto → enviado → bloque muestra "Activando"
7. Forzar `faceEnrollStatus: 'enrolled'` → reload → "Acceso activo"
8. **Block 2:** monto + fecha próximo cobro + historial (al menos 1 fila si startDate en pasado)
9. **Block 3:** datos del espacio, grid 2 columnas
10. **Block 4:** "Cambiar de tamaño" → modal → seleccionar → solicitud enviada
11. **Block 4:** "Cancelar reserva" → confirmación con fecha → "Cancelar definitivamente" → badge naranja
12. En `#/portal` → 1 reserva → redirige directo
13. Hacer 2da reserva → `#/portal` → muestra selector con 2 tarjetas
14. 0 reservas (borrar localStorage) → empty state con "Reservar ahora"

- [ ] **Step 10.4: Commit final**

```bash
git add home.jsx styles.css
git commit -m "feat: portal v2 completo — acceso facial, pagos, gestionar, resize, cancelar"
```

- [ ] **Step 10.5: Sincronizar con GitHub**

```bash
# Copiar home.jsx y styles.css al clone de GitHub
# (ejecutar desde PowerShell)
Copy-Item "C:\Users\augus\micofrontend-v3\home.jsx" "C:\Users\augus\tmp\micofrontend-fresh\home.jsx" -Force
Copy-Item "C:\Users\augus\micofrontend-v3\styles.css" "C:\Users\augus\tmp\micofrontend-fresh\styles.css" -Force

cd C:\Users\augus\tmp\micofrontend-fresh
git add home.jsx styles.css
git commit -m "feat: portal cliente v2 — acceso facial, pagos, cambio de tamaño, cancelación"
git push origin main
```

---

## Verificación final del plan

- [ ] Spec § 1 (flujos de entrada): cubierto por Task 2 (PortalEntry 0/1/2+)
- [ ] Spec § 2 (pantalla de reserva): cubierto por Task 3 (shell) + Tasks 4–9 (bloques)
- [ ] Spec § 3 (acceso facial 4 estados): cubierto por Task 4
- [ ] Spec § 4 (enrollment flow 3 pasos): cubierto por Task 5
- [ ] Spec § 5 (consentimiento biométrico Ley 25.326): cubierto por Task 5 Step 5.1
- [ ] Spec § 6 (bloque pagos + historial): cubierto por Task 6
- [ ] Spec § 7 (bloque tu espacio): cubierto por Task 7
- [ ] Spec § 8 (gestionar resize): cubierto por Task 9
- [ ] Spec § 8 (gestionar cancel 2 pasos): cubierto por Task 8
- [ ] Spec § 9 (empty state 0 reservas): cubierto por Task 2
- [ ] Spec § 10 (selector 2+ reservas): cubierto por Task 2
- [ ] Spec § 11 (notificaciones al equipo): localStorage mock — resize requests en `mc.resize_requests`. Nota: la notificación real (email via Resend) se implementa en un plan posterior cuando el backend esté conectado.
- [ ] Spec § 12 (rutas de hash): cubierto por Task 2
- [ ] Spec § 13 (estados): `cancellation_scheduled` cubierto por Tasks 2 y 8
